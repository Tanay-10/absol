Replay artifacts directory.

Place raw or normalized JSONL files here for deterministic demo replays.
These files are generated by running the pipeline and can be re-ingested
without hitting live APIs.
